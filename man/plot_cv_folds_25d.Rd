% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_cv_folds_25d}
\alias{plot_cv_folds_25d}
\title{Plot generated CV folds in a 2.5D scatterplot}
\usage{
plot_cv_folds_25d(covars, cv_index, max_points = 1200L, seed = 2026L)
}
\arguments{
\item{covars}{A \code{stdt} object (recommended) or a \code{data.frame}/\code{data.table}
that contains \code{lon} and \code{lat} columns.}

\item{cv_index}{Fold assignment vector returned by \code{\link[=generate_cv_index]{generate_cv_index()}}.
Must have length \code{nrow(covars$stdt)} for \code{stdt} input or \code{nrow(covars)}
for table-like input.}

\item{max_points}{Maximum number of points to display (default \code{10000}).}

\item{seed}{Random seed used when downsampling.}
}
\value{
A \code{plotly} 3D scatter object.
}
\description{
Creates a 2.5D scatterplot (x, y, fold index as z) with fold/cluster
number shown by color. For large datasets, points are downsampled to
at most \code{max_points} for display.
}
\examples{
data(spdat)
cv_index <-
  generate_cv_index(spdat, cv_mode = "lolo")

if (requireNamespace("plotly", quietly = TRUE)) {
  p <- plot_cv_folds_25d(spdat, cv_index)
  p
}
}
