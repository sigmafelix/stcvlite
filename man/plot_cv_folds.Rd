% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_cv_folds}
\alias{plot_cv_folds}
\title{Plot generated CV folds}
\usage{
plot_cv_folds(covars, cv_index, max_points = 1200L, seed = 2026L)
}
\arguments{
\item{covars}{A \code{data.frame}/\code{data.table} (or inheriting object) that
contains \code{lon} and \code{lat} columns.}

\item{cv_index}{Fold assignment vector returned by \code{\link[=generate_cv_index]{generate_cv_index()}}.
Must have length \code{nrow(covars)}.}

\item{max_points}{Maximum number of points to display (default \code{10000}).}

\item{seed}{Random seed used when downsampling.}
}
\value{
A \code{plotly} 3D scatter object.
}
\description{
Creates a plotly scatterplot (x, y, fold index as z) with fold/cluster
number shown by color. For large datasets, points are downsampled to
at most \code{max_points} for display.
}
\examples{
data(spdat)
cv_index <-
  generate_cv_index(spdat, cv_mode = "lblo", cv_fold = 6)

if (requireNamespace("plotly", quietly = TRUE)) {
  p <- plot_cv_folds(spdat, cv_index)
  p
}
}
